package org.example;

public abstract class PrivateTransport extends Transport {
    public PrivateTransport(int capacity, FuelTypes fuelTypes, Location location) {
        super(capacity, fuelTypes, location);
    }
}
